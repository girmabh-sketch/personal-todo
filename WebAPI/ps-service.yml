apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None # This makes the service headless (useful for StatefulSet)
  selector:
    app: postgres
  ports:
    - name: "postgres"
      protocol: TCP
      port: 5432